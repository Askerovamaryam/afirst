#include <iostream>
#include <vector>
int main()
{
	
std::vector<std::vector<int>> nam_function(std::vector<std::vector<int>>& matrix);

	int a=matrix.size();
	int b=matrix[0]/;
	//std::cin>>a;
	//std::cin>>b;
	std::vector <std::vector<int>>matrix2(a, std::vector<int> (b));
	for(int i=0;i<a;i++){
	for(int j=0;j<b;j++){
		matrix[j][i]=matrix[i][j];
	}
}
		
	PrintMatrix(matrix 2);
	
    
	//std::vector <std::vector<int>>(const std::vector<std::vector<int>>& matrix);
	
	return 0;
}
	
	
